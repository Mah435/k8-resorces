appVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: default
  labels: 
    name: nginx-service
spec:
  selector:
    environment: dev
  ports:
   -name: http-port
    protocol: TCP
    port: 80
    targetPort: 80
 